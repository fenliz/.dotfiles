return function(opts)
	-- local pid = vim.fn.getpid()
	-- local omnisharp_bin = '/home/charlie/.local/share/nvim/lsp_servers/omnisharp/run'
	--
	-- opts.cmd = { omnisharp_bin, '--languageserver', '--hostPID', tostring(pid) }
	-- opts.root_dir = require'lspconfig'.util.root_pattern('*.csproj', '*.sln')
end
