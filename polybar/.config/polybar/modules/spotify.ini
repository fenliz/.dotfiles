[module/spotify]
type = custom/script
exec = python3 ~/.config/polybar/scripts/spotify-status.py -f '{play_pause} {artist}: {song}' --font=0 -t 50

interval = 1

format-prefix = "  "
format = <label>
format-padding = 5

click-left = playerctl --player=spotify play-pause 
click-right = playerctl --player=spotify next 
click-middle = playerctl --player=spotify previous 
